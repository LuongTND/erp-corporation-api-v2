# Project Rules

## General
- Language: **C# 14** (.NET 10). Use C# 14 features where appropriate.
- Use string interpolation (`$"..."`) over concatenation (`+`) or `string.Format`.
- Clean Architecture boundaries. Correctness over speed.
- Add to existing files before creating new ones.
- No generic names (`Service`, `Manager`, `Helper`) — use `AuthService`, `UserRepository`.
- Primary constructor injection only. Never inject `IServiceProvider`.
- Self-service commands/queries (employee acting on their own data) use the `My` prefix: `GetMyStore`, `UpdateMyProfile`. Admin-scoped equivalents targeting an arbitrary user ID do not use `My`.

## Layers
| Layer | Contains |
|---|---|
| Domain | Entities, Enums, Value Objects |
| Application | Command/Query Handlers, Validators, `IUnitOfWork`, `IGenericRepository<T>`, infrastructure contracts, business logic |
| Infrastructure | `UnitOfWork`, `GenericRepository<T>`, DbContext, external provider implementations |
| API | Controllers, Middlewares, Filters |
| Contract | Shared types: `ApiResponse<T>`, Exceptions, `RegisterServiceAttribute`, `AutoInjectionExtensions`, Response DTOs (`*Response`, `*Dto`) |

**Dependency direction (one-way only):**
```
API → Application → Domain
API → Infrastructure → Application → Domain
Contract ← all layers
```

- Application must **not** reference Infrastructure.
- Infrastructure references Application (to implement interfaces defined there).
- Controllers inject `ISender` only — never Repositories, DbContext, or Services directly.

## DI Registration
Use `[RegisterService(typeof(IXxx))]` on the class. Never register manually in `Program.cs`.

```csharp
[RegisterService(typeof(IAuthService))]
public sealed class AuthService(IUnitOfWork unitOfWork) : IAuthService { }
```

Manual registration allowed **only** for: `AddDbContext`, `AddMediatR`, `AddAuthentication`, `AddHttpClient`, `Configure<TOptions>`.

| Lifetime | Use for |
|---|---|
| `Scoped` *(default)* | Services, Repositories, UnitOfWork, anything using `DbContext` |
| `Singleton` | Stateless helpers — must not depend on `DbContext` |
| `Transient` | Lightweight stateless classes |

## CQRS (primary pattern)
- **Command** = write → returns `Guid` or `Unit`. Never returns entity.
- **Query** = read → returns DTO. Never returns entity.
- Business logic lives in Handlers, not Controllers or Repositories.
- Handler calls `IUnitOfWork`. Call `EnsureSaveAsync` once — not inside Repository.
- Query handlers may use `DbContext` directly for `ProjectToType<TDto>()` projections.
- Folder: `Application/Features/<Domain>/<Feature>/` — 1 feature per folder.

## Models placement
| Type | Location | Reason |
|---|---|---|
| Response DTOs (`*Response`, `*Dto`) | `Contract/Models/<Domain>/` | Shared across API, Application, tests |
| Commands / Queries (`*Command`, `*Query`) | `Application/Features/<Domain>/<Feature>/` | Only Application uses them |
| Filter / pagination inputs | `Application/Features/<Domain>/<Feature>/` | Query-specific, not shared |

Never put Response DTOs in `Application/Features/` — they belong in `Contract`.
- No `Shared/` carve-out in `Application/Features/`. If a DTO is reused across more than one handler or feature, move it to `Contract/Models/<Domain>/`.

## Services (shared infra only)
Use Services **only** for cross-cutting concerns shared across Handlers: email, SMS, file storage, external APIs. Never use as a CRUD wrapper.

## Controllers
- Inject `ISender` only. No business logic.
- Return `ActionResult<ApiResponse<T>>`. Use `Ok(ApiResponse<T>.Ok(result))`.
- Never catch exceptions — global middleware handles all errors.
- No `[FromServices]` in action parameters. Ambient context (current user ID, tenant, etc.) must be resolved inside the handler via `IUserContext` or equivalent contract interface.
- Every action **must** have a `/// <summary>` XML doc comment (one line, tiếng Việt) describing what the endpoint does — shown in Swagger.

```csharp
[HttpPost]
public async Task<ActionResult<ApiResponse<Guid>>> Create(CreateXxxCommand cmd, CancellationToken ct)
    => Ok(ApiResponse<Guid>.Ok(await sender.Send(cmd, ct)));
```

## Mapping
- Mapster only. Config in Application layer.
- Use `entity.Adapt<TDto>()` or `queryable.ProjectToType<TDto>()`.
- Never map manually in Controllers or Repositories.
- Do NOT inject `IMapper`/`ServiceMapper`. Use `.Adapt<T>()` directly.
- Custom maps go in `IRegister` classes (`*MappingConfig.cs`) colocated with the feature handler — no global `Mappings/` folder.

## Async
- `async/await` + `CancellationToken` on all DB and external calls.
- Method names end with `Async`. Never `.Result` or `.Wait()`.

## Error Handling
Throw custom exceptions only — never `Exception`.

| Exception | Status |
|---|---|
| `BadRequestException` | 400 |
| `UnauthorizedException` | 401 |
| `ForbiddenException` | 403 |
| `NotFoundException` | 404 |
| `ConflictException` | 409 |
| `TooManyRequestException` | 429 |
| `DatabaseOperationException` | 500 |

All inherit `AppException` in `Contract/Exceptions/`.

**Tất cả message exception phải viết bằng tiếng Việt.** Không dùng tiếng Anh trong message trả về client.

## Validation
- FluentValidation validators in Application layer.
- `ValidationPipelineBehavior` auto-runs via MediatR pipeline — throws on failure.
- Business validation requiring DB checks goes in Command Handlers.

### Rules by operation

| Operation | Validator | Handler |
|---|---|---|
| **C** (Create) | Required fields, format, range, enum | FK existence check, uniqueness, business pre-conditions |
| **R** (Read) | Id format (if route param) | `NotFoundException` if entity not found |
| **U** (Update) | Same as Create for editable fields | Load entity → `NotFoundException`, ownership/status check before mutating |
| **D** (Delete) | Id format | `NotFoundException`, guard cascading damage (e.g., InProgress instances) |

- Validators check **shape only** (not-null, length, regex, range) — never hit the DB.
- Handlers own **semantic** validation: FK existence, duplicate detection, state-machine guards.
- For status transitions: assert current status is a valid source state before updating (`BadRequestException` if not).

### Checklist bắt buộc cho mọi feature Create/Update/Delete

**Create:**
- [ ] Validate required fields (Validator)
- [ ] Validate FK existence — mọi foreign key đều phải check tồn tại trước khi insert
- [ ] Check trùng lặp (unique fields) → `ConflictException`
- [ ] Validate enum values hợp lệ (Validator: `IsInEnum()`)

**Update:**
- [ ] Load entity → `NotFoundException` nếu không tìm thấy
- [ ] Validate FK existence nếu field FK có thể thay đổi
- [ ] Check trùng lặp (unique fields, loại trừ chính nó) → `ConflictException`
- [ ] Không cho phép update entity đã soft-delete → `BadRequestException`

**Delete:**
- [ ] Load entity → `NotFoundException` nếu không tìm thấy
- [ ] Guard cascade: kiểm tra entity có đang được tham chiếu bởi entity khác không → `ConflictException` với message rõ ràng
- [ ] Không hard-delete — dùng soft-delete (`IsDeleted = true`) trừ khi có lý do cụ thể
